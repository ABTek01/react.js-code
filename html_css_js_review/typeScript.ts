/*
    create single variable types in ts
*/
let exampleBool: boolean = true

let exampleString: string = 'typeScript'

let exampleNum: number = 3

/*
    create multiple variable types

    can use "|" pipe to assign 
    multiple variable types.
*/

let multiExample0: boolean | string = 'multiple variable type'

let multiExample1: number | boolean = false

let multiExample2: string | number = 1

/*
    implicit vs explicit typing
*/
//explicit; all data is transparent and forthcoming.
//explicit reason?; variable remains a string type.
let explicitEx: string;
explicitEx = 'explicit variable'

//implicit; all data maybe indirect and questionable.
//implicit reason?; variable can be reassigned
let implicit = 'implied string'

/*
    type checking
*/







